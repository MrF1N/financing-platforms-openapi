rootProject.name = 'financing-platforms-openapi'
include 'currency-com'
include 'commons'
include 'banks-by'

rootProject.children.each {project ->
    project.buildFileName = "${project.name}.gradle"
}